# Upstash REST credentials for temporary room snapshots.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
# Native Redis URL enables Socket.IO pub/sub across Vercel instances.
REDIS_URL=
VITE_SOCKET_URL=
# Canonical public origin used when copying room invite links.
VITE_APP_URL=http://localhost:5173
